name: Distributed harness verification
on:
  push:
    branches: [feat/distributed-harness-20260911]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install locked Python environment
        run: |
          python -m pip install uv
          uv sync --frozen
      - name: Python regression tests
        run: |
          mkdir -p test-results
          uv run pytest -m 'not real_gateway' --junitxml=test-results/python-baseline.xml -q
      - name: Node regression tests
        run: npm test
      - name: Package source and evidence
        if: always()
        run: |
          mkdir -p test-results
          git rev-parse HEAD > test-results/commit.txt
          git archive --format=tar HEAD > test-results/source.tar
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: distributed-harness-evidence
          path: test-results/
          retention-days: 14
