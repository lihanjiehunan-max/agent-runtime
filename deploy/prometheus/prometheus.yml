global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: enterprise-agent-runtime
    metrics_path: /metrics
    static_configs:
      - targets:
          - runtime-api:8000
